/**/#
/**/# rpcsvc - include files for interfaces to remote execution functions
/**/#

all::
depend::

/*
 * Install header files.
 */

HEADERS		= \
	rstat.h

MakeDirectories(install,$(INCDIR)/Std/rpcsvc)
InstallMultipleFlags($(HEADERS),$(INCDIR)/Std/rpcsvc,$(INSTINCFLAGS))
