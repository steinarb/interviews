/**/#
/**/# netinet - include files for the internet system
/**/#

all::
depend::

/*
 * Install header files.
 */

HEADERS		= \
	in.h

MakeDirectories(install,$(INCDIR)/Std/netinet)
InstallMultipleFlags($(HEADERS),$(INCDIR)/Std/netinet,$(INSTINCFLAGS))
