XCOMM
XCOMM internal include files for using old 2.6 classes' names without prefixes
XCOMM

HEADERS = *.h
DEST = $(INCDIR)/InterViews/2.6
SUBDIRS = InterViews

InstallMultipleFlags($(HEADERS),$(DEST),$(INSTINCFLAGS))
MakeInSubdirs($(SUBDIRS))
